package com.microsoft.egh.model;

import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;

public interface ClientFactRepository extends JpaRepository<ClientFact, Long> {
}
